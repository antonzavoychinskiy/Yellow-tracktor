#pragma once

#include "esp_err.h"

/*
 * Экраны интерфейса (FR-41..46): 6 экранов + постоянная строка
 * статуса (FR-42) + баннер критического предупреждения / отладочный
 * баннер (FR-43, NFR-17). Один LVGL-экран с переключаемыми
 * контейнерами-страницами — не отдельные lv_scr_load(), см.
 * ui.c.
 *
 * ПРИМЕЧАНИЕ: в требованиях (FR-41, строка "Карточка маршрута")
 * заявлено, что карточка показывает "число точек, длина" — но по
 * протоколу (Приложение А.2) MLIST отдаёт только номер и название;
 * число точек/длина появляются лишь в ответе MLOAD ПОСЛЕ загрузки
 * (FR-25). Карточка здесь показывает то, что реально доступно на этот
 * момент (название, предупреждение, подсказка); число точек/длина —
 * на экране "Результат загрузки", где они действительно есть.
 */

esp_err_t ui_init(void);

/* Вызывать из ui_task каждый тик: обновляет данные и вызывает
 * lvgl_port_tick_and_handle(). */
void ui_tick(void);
